# TEST_GET_REPO_BUG boolean variable defines if test 'test_get_repo_name_bug_scenario'
# in file 'test_7_update_repository_bug.py' should actually be run
# - when False then test will always Pass (test will immediately return without errors so will Pass)
# - when True then test will execute (and Fail)
TEST_GET_REPO_BUG = False